worker-threads: 4
worker-fs-threads: 2
worker-grpc-threads: 2
logger-level: debug

is-testing: true

server-port: 8080

note-sync-client:
  endpoint: $grpc_mockserver

langchain-client:
  endpoint: $grpc_mockserver

tag-recommender-client:
  endpoint: $grpc_mockserver

dbconnection: 'postgresql://testsuite@localhost:15433/noteluoto_db_1'
